[platformio]
src_dir = .

[env:teensy41_xavier]
platform = teensy@4.18.0
board = teensy41
framework = arduino
monitor_speed = 115200

; The sketch uses the Servo library bundled with Teensyduino.
build_src_filter =
  +<steering_servo_test.ino>
  -<.pio/>
