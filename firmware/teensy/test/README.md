# Firmware tests

Host-side unit tests for PID math, command parsing, saturation, and watchdog state transitions will be added here as those modules are implemented. Hardware-in-the-loop tests belong in a separate `test_embedded` PlatformIO environment so they cannot be confused with desktop tests.
