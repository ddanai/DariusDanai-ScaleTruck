# scale_truck_firmware_bridge

ROS 2 bridge package for communicating with the Teensy low-level controller over serial or another selected transport.

Expected contents:

- `src/`: bridge node source files.
- `config/`: serial and hardware interface configuration.

