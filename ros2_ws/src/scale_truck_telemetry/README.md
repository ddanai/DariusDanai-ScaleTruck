# scale_truck_telemetry

Telemetry bridge package for publishing scale truck state to external monitoring or digital twin systems.

Expected contents:

- `src/`: telemetry bridge node source files.
- `config/`: telemetry endpoint and runtime configuration.

