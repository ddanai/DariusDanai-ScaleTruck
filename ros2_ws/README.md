# ROS 2 Workspace

This workspace contains the ROS 2 packages for the scale truck migration.

Build from this directory once package metadata and build files are added:

```bash
colcon build --symlink-install
source install/setup.bash
```

